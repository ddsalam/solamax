# Secrets — JANGAN pernah commit kredensial (lihat CLAUDE.md aturan #3)
.env
.env.*
!.env.example
*.local
config.local.*
secrets/
*.key
*.pem

# Agent buffer lokal (offline queue)
*.sqlite
*.sqlite3
buffer/

# Dependencies / build (untuk app yang dibuat nanti)
node_modules/
dist/
build/
.next/
coverage/

# OS / editor
.DS_Store
*.log
