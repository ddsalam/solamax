# Salin ke .env (DI-GITIGNORE) lalu isi. JANGAN commit .env.
# Lokal/dev (via cloud-sql-proxy atau Postgres lokal):
DATABASE_URL="postgresql://USER:PASSWORD@127.0.0.1:5432/solamax?schema=public"
# Cloud Run (unix socket Cloud SQL):
# DATABASE_URL="postgresql://USER:PASSWORD@localhost/solamax?host=/cloudsql/solamax:asia-southeast2:solamax-pg&schema=public"
