# Salin ke .env.local (DI-GITIGNORE oleh Next & repo). JANGAN commit.
# Review lokal: jalankan cloud-sql-proxy dulu, lalu pakai user yang sama dgn backend
# (atau lebih baik: user read-only khusus — lihat README).
DATABASE_URL="postgresql://USER:PASSWORD@127.0.0.1:5432/solamax?schema=public"
