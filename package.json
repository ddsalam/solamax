{
  "name": "solamax",
  "private": true,
  "version": "0.0.0",
  "description": "SolaMax \u2014 pipeline EasyMax POS \u2192 dashboard pengawasan SPBU SolaGroup",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "agent": "pnpm --filter @solamax/agent",
    "typecheck": "pnpm -r typecheck",
    "check": "node scripts/ci/check-sql-syntax.mjs && bash scripts/ci/mutasi.selftest.sh && pnpm -r typecheck && pnpm -r test",
    "check:sql": "node scripts/ci/check-sql-syntax.mjs"
  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "pg-query-emscripten": "^5.1.0"
  }
}
